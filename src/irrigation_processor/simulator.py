def irrigation_simulator(context, calibrated_path, preprocessed_path):
    print("simulating...", context, calibrated_path, preprocessed_path)
    simulated_path = "/path/to/simulated_data"
    print("simulation complete...", simulated_path)
    return simulated_path
